rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All reads/writes go through the admin SDK server-side (functions) or
    // via a per-file download token — nothing needs direct client access.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
