rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Functions backend (server-side, uses Admin SDK
    // which bypasses these rules). No direct client access to Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
